/**
 * Calculate the percentage.
 * @param {number} total Total values.
 * @param {number} number Reference value.
 * @returns {number} Reference value.
 */
function percentage(total, number) {
  console.log((number * 100) / total);
  return (number * 100) / total;
}
